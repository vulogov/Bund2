[package]
name = "bund2-ir"
description = "BundIR: linear, span-carrying, effect-annotated IR. See RFC-0003."
version.workspace      = true
edition.workspace      = true
license.workspace      = true
repository.workspace   = true
rust-version.workspace = true

[dependencies]
bund2-value.workspace = true
bund2-syntax.workspace = true

[features]
# Exposes `Fragment::new_unchecked`, so a test can hold a fragment `new` would
# refuse and show the executor refuses it too. No shipped crate enables it.
unchecked = []

[lints]
workspace = true
