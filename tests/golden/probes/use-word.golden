# golden for tests/probes/use-word.bund
# captured by `cargo xtask golden` from reference/Bund via target/oracle
# normalised: id and stamp (F14), dict member order (F15), ANSI stripped
# verified: two oracle runs produced identical output
## exit
0
## output
probe: use loads a library by file:// URL and keeps what it registers
library loaded
42
probe: use. takes the URL from the workbench
library loaded
42
╭╮
╰╯
╭╮
╰╯
