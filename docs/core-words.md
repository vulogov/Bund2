# D14 core/library partition (generated)

Generated by `cargo xtask scope --write`. Do not edit.

D14 records method B″: the corpus and the authored probes seed the core set; it is closed over implementation reach and D18 workbench forms; and a file with any core word is wholly core **in `vm/` and `stack/` only**. The Bund runtime is where the standard library lives, so a partly-used file there is a partly-used library, not a partly-used language feature.

Core 286 / library 211 of 497 in scope.

## core

- **bund/bund** — ?class ?lambda ?object ?word apply args args.parse bund.eval bund.eval. bund.exit compile exit lambda* lambda= load.model save.model use use.
- **bund/conditional** — ?error ?ifthenelse ?try context csv curry fmt raise sqlite
- **bund/debug_fun** — debug.display_hostinfo debug.display_stack
- **bund/filesystem** — cwd file file. fs.cwd fs.ls fs.ls. fs.rm ls ls. rm
- **bund/generators** — generator generator.sample generator.sample*
- **bund/graph** — graph graph! graph. graph.allpath graph.path graph.paths graph.transitiveclosure
- **bund/io** — bund.prompt input input* io.banner io.banner. io.graph io.graph.
- **bund/math** — math.interpolation math.securerandom.int seq
- **bund/oop** — # #. is unwrap wrap
- **bund/string** — id.uuid lines lines. string.distance.levenshtein string.distance.levenshtein. string.expressionmatch string.expressionmatch. string.random.lorem string.random.lorem. string.random.name string.random.name. string.tokenize string.tokenize. string.tokenize.lines string.tokenize.lines. string.wrap.english string.wrap.english.
- **bund/sysinfo** — sysinfo.version version
- **bund/system** — display sleep sleep.seconds system.setproctitle system.setproctitle.
- **bund/values** — ++ +++. ++. call, get, make.call make.call. merge merge. pull pull. push. sort sort. unfold unfold.
- **stack/drop** — drop drop_in drop_stack
- **stack/dup** — dup dup_many dup_many_in dup_one dup_one_in
- **stack/swap** — swap swap_in swap_one
- **stack/workbench** — $ . return return_from return_to take
- **vm/artefacts** — ? class complex conditional config dict false lambda list match metrics nodata pair ptr sample text true valuemap | Λ λ ∅
- **vm/artefacts_json** — json
- **vm/bund_object** — object
- **vm/convert** — convert.to_bool convert.to_bool. convert.to_dict convert.to_dict. convert.to_float convert.to_float. convert.to_int convert.to_int. convert.to_list convert.to_list. convert.to_matrix convert.to_matrix. convert.to_string convert.to_string. convert.to_textbuffer convert.to_textbuffer. matrix matrix.
- **vm/execute** — ! !. execute execute.
- **vm/json** — json.path
- **vm/lambdas** — register unregister
- **vm/logic** — != *loop *loop. < <= == > >= ?false ?false* ?false*. ?false. ?stack ?true ?true* ?true*. ?true. and for for. if if. if.false if.false.in_workbench if.in_workbench if.stack ifthenelse ifthenelse. loop loop. map map. not notifthenelse notifthenelse. or times times. while while. ≠ ⩽ ⩾
- **vm/math** — * ** **. *+ *+. *- *-. *. */ */. + +. - -. / /. Pi float.+Inf float.-Inf float.E float.NaN float.Pi math.abs math.acos math.asin math.atan math.cbrt math.ceil math.cos math.cosh math.floor math.fract math.round math.signum math.sin math.sinh math.sqrt math.tan math.tanh Σ Σ. π
- **vm/print** — nl print print. println println. space
- **vm/stackop** — ?. ?move
- **vm/string** — format format.
- **vm/values** — , ?key at at. attribute car car. cdr cdr. get head head. len set tag tail tail. ∈
- **vm/vars** — var var- var?

## library

- **bund/bund** — !! ?alias ?stdlib alias= bootstrap bund.eval-file bund.eval-file. lambda! load load.aliases load.lambdas load.script load.stacks save save.aliases save.lambdas save.script save.stacks
- **bund/convert** — convert.from_html convert.from_html.
- **bund/debug_fun** — debug debug.display_distributed_info debug.display_memstat debug.display_workbench debug.dump debug.shell log.debug log.error log.info log.trace log.warning
- **bund/encoding** — decode.base64 decode.base64. encode.base64 encode.base64.
- **bund/filesystem** — cp file.write file.write. filename filename. fs.cp fs.is_file fs.is_file. fs.ls.dir fs.ls.dir. fs.ls.files fs.ls.files. fs.mv fs_is_file. mv stdin stdin. url url.
- **bund/io** — io.textfile io.textfile. password
- **bund/math** — math.cosecant math.exp math.factorial math.ln math.log10 math.normalize math.normalize, math.normalize. math.normalize., math.nroot math.perimeter math.power math.random.int math.smoothing math.smoothing, math.smoothing. math.smoothing., seq.asc seq.desc
- **bund/oop** — False Floats Intervals List True
- **bund/string** — id.ulid string.deunicode string.deunicode. string.distance string.distance. string.distance.dameraulevenshtein string.distance.dameraulevenshtein. string.distance.hamming string.distance.hamming. string.distance.jarowinkler string.distance.jarowinkler. string.distance.sift3 string.distance.sift3. string.fuzzymatch string.fuzzymatch. string.grok string.grok. string.prefix string.prefix. string.random.fullname string.random.fullname. string.random.ipv4 string.random.ipv4. string.random.lastname string.random.lastname. string.random.password string.random.password. string.random.phone string.random.phone. string.random.word string.random.word. string.regex string.regex. string.regex.matches string.regex.matches. string.regex.split string.regex.split. string.suffix string.suffix. string.tokenize.stemmed string.tokenize.stemmed. string.tokenize.unique string.tokenize.unique. string.wildcard string.wildcard.
- **bund/sysinfo** — sysinfo.hostname sysinfo.kernel_version sysinfo.mem.buffers sysinfo.mem.buffers. sysinfo.mem.cached sysinfo.mem.cached. sysinfo.mem.free sysinfo.mem.free. sysinfo.mem.shared sysinfo.mem.shared. sysinfo.mem.total sysinfo.mem.total. sysinfo.mem.used sysinfo.mem.used. sysinfo.os_version sysinfo.system sysinfo.virtualization sysinfo.virtualization?
- **bund/system** — sh sh. system.ip system.ipv6 system.locale system.path.filename system.path.filename. system.path.split system.path.split. system.shell system.shell.
- **bund/values** — +++ get., pull.workbench pull.workbench. push set, set., unique unique.
- **stack/clear** — clear clear_in
- **stack/current** — current to_current to_stack
- **stack/ensure_stack** — ensure_stack ensure_stack_with_capacity stack stack_exists
- **stack/fold** — fold fold_stack
- **stack/rotate** — --> -> <-- rotate_current_left rotate_current_right rotate_stack_left rotate_stack_right stacks_right →
- **stack/stack_move** — move move_from
- **vm/alias** — alias unalias
- **vm/create_aliases** — <- ←
- **vm/ctx** — endcontext
- **vm/json** — json.from_value json.to_value
- **vm/lambdas** — resolve resolve.class
- **vm/logic** — do do.
- **vm/stacks** — clear_stacks drop_stacks
- **vm/string** — concat_with_space sp string.camel string.lower string.snake string.title string.upper
- **vm/time** — time.now time.timestamp
- **vm/values** — ?type type type.of
